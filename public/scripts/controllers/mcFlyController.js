myApp.controller('McFlyController', ['$scope', function($scope) {
    $scope.message = '"The way I see it, if you\'re going to build a time machine into a car, why not do it with some style?" -- Doc';

}]);